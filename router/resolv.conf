# Static resolver configuration bind-mounted into the application container.
# Points at the router's local unbound, which forwards over TCP through the
# tunnel. This bypasses Docker's embedded resolver (127.0.0.11), whose upstream
# lookups would otherwise leak past the tunnel.
nameserver 127.0.0.1
options timeout:2 attempts:2
